{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.isort",
                "ms-python.vscode-pylance",
                "njpwerner.autodocstring",
                "bierner.markdown-mermaid",
                "github.copilot-chat",
                "ms-azuretools.vscode-docker",
                "tamasfe.even-better-toml",
                "redhat.vscode-yaml",
                "charliermarsh.ruff",
                "esbenp.prettier-vscode",
                "dbaeumer.vscode-eslint"
            ]
        }
    },
    "mounts": [
        "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind",
        "source=${localWorkspaceFolderBasename}-venv,target=${containerWorkspaceFolder}/.venv,type=volume",
        "source=${localWorkspaceFolderBasename}-node_modules,target=${containerWorkspaceFolder}/frontend/node_modules,type=volume"
    ],
    "features": {
        "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {},
        "ghcr.io/devcontainers/features/github-cli:1": {},
        "ghcr.io/devcontainers/features/copilot-cli:1": {}
    },
    "runArgs": [
        "--network=host",
        "--add-host=host.docker.internal:host-gateway"
    ],
    "postAttachCommand": "sudo chown -R vscode:vscode ./.venv && sudo chown -R vscode:vscode ./frontend/node_modules"
}